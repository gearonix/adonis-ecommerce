import {FC} from 'react';
import {Button} from "shared/ui";

const AddToSavedCart: FC = () => {
    return <Button w={'114px'} h={'32px'} color={'blueBordered'}>Save for later</Button>
}

export default AddToSavedCart
