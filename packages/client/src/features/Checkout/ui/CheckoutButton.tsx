import { FC } from 'react'
import { Button } from 'shared/ui/kit'

const CheckoutButton: FC = () => {
  return <Button color={'green'} w={'100%'} h={'54px'}>Checkout</Button>
}

export default CheckoutButton
