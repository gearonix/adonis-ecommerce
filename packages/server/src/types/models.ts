export enum Lengths {
    TITLE = 40,
    IMAGE = 200,
    LONGTEXT = 60,
    ID = 8,
    TINYTEXT = 10,
    HASH = 100,
    TOKEN = 300
}

export enum Roles {
    SALESMAN = 'SALESMAN',
    CUSTOMER = 'CUSTOMER'
}
